/**
 * 评价列表
 */
import evaluationList from './src/evaluation-list.vue'

evaluationList.install = function (Vue) {
  Vue.component(evaluationList.name, evaluationList)
}

export default evaluationList
